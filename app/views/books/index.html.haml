%h3 Books

%table.table.table-condensed
  %thead
    %tr
      %th Title
      %th Isbn
      %th Page count
      %th Year
      %th Category
      %th Publisher name
      %th Author name
      %th
      %th
      %th
  %tbody
    - @books.each do |book|
      %tr
        %td= book.title
        %td= book.isbn
        %td= book.page_count
        %td= book.year
        %td= book.category
        %td= book.publisher_name
        %td= book.author_name
        %td= link_to 'Show', book, :class => 'btn btn-mini'
        %td= link_to 'Edit', edit_book_path(book), :class => 'btn btn-mini'
        %td= link_to 'Destroy', book, :method => :delete, :data => { :confirm => 'Are you sure?' }, :class => 'btn btn-mini'

- # TODO FIXME új könyvet csak az admin vehet fel
- #= link_to 'New Book', new_book_path, :class => 'btn btn-mini'
